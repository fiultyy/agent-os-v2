"""Control subsystem.

NOTE: InterceptLayer / ReasoningLayer removed in Phase 2 L6 dead-code cleanup
(D-15 三层控制 deferred). Module kept as empty placeholder to preserve the
``src.control`` package; safe to delete the directory wholesale if no further
control-layer work is planned.
"""
